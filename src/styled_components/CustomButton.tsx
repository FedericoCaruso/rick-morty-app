import styled from 'styled-components';

const CustomButton = styled.button`
    font-weight: bold;
    font-size: 1rem;
    padding: 10px;
    width: 25%;
    borderRadius: 8px;
`;

export default CustomButton;
