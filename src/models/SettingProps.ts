export interface ISettingsProps {
    theme: {
        backgroundColors: {
            mainColor: string;
            secondaryColor: string;
        },
        textColors: {
            mainColor: string;
            secondaryColor: string;
        }
    }
}
